# YQS0｜Human Review Card v0.1

Status: `candidate_started`

Human decision token if accepted:

`ACCEPT_YQS0_SUPABASE_SOVEREIGN_RESEARCH_KERNEL_ARCHITECTURE`

Acceptance approves the **architecture candidate only**. It does not authorize merge, Supabase project creation, migrations, credentials, data import, runtime cutover, Registry promotion, capital action or live execution.

---

## 1. Decision being requested

Approve or reject the following architecture:

> Supabase becomes Yuanli Quant's Research State Kernel / Operational Memory / Control Plane; GitHub retains normative Canon authority; external quant workers retain heavy deterministic compute; raw/restricted evidence remains in an independent evidence vault/object layer; Human Gate retains authorization.

---

## 2. Twelve review questions

Mark each `PASS / FAIL / NEEDS_REVISION`.

1. **Authority separation** — Is `GitHub = normative Canon authority` preserved with no automatic Supabase-to-Canon write path?
2. **ME1 compatibility** — Are `ResearchTarget / EngineThesis / PositionPassport / BookState` reused without semantic redefinition?
3. **Supabase scope** — Is Supabase limited to state, memory, evidence metadata, PIT, runtime control, replay, governance ledger and projections rather than heavy compute or live trading?
4. **Dedicated sovereign domain** — Is `yuanli-invest-runtime` correctly isolated from `yuanli-health`?
5. **Eight-schema design** — Are `core/evidence/pit/quant/runtime/replay/governance/projection` sufficient and non-overlapping for YQS0?
6. **Eighteen-object minimality** — Is the frozen object set enough to implement the kernel without prematurely creating a large enterprise schema?
7. **PIT enforcement** — Does the PIT law make historical knowability a database-access rule rather than a prompt convention?
8. **Revision integrity** — Are revised/latest values prevented from silently contaminating historical replay?
9. **RLS authority** — Are `quant_worker` and `system` technically unable to act as human approval identities?
10. **Compute boundary** — Is `Supabase control plane / external quant compute plane` the correct division for factor, replay, benchmark and ML workloads?
11. **Append-first learning/governance** — Do run receipts, replay settlements and authority events preserve history rather than overwrite it?
12. **No scope smuggling** — Are ME2-ME5, QXM3, A9 operational-canon switch, evidence/outcome admission, portfolio sizing and live execution still explicitly unauthorized?

---

## 3. Architecture facts to verify

### Eight schemas

`core / evidence / pit / quant / runtime / replay / governance / projection`

### Eighteen core objects

- core: 4
- evidence: 4
- pit: 3
- quant: 2
- runtime: 1
- replay: 2
- governance: 1
- projection: 1

### Six application roles

`principal / researcher / quant_worker / reviewer / viewer / system`

### Dual-authority law

```text
GitHub -> accepted contract/schema projection -> Supabase
Supabase -> proposal package -> Human Review -> GitHub PR
```

There is no automatic reverse Canon mutation.

---

## 4. What acceptance would authorize next

Only the preparation of **YQS1｜Supabase Research Kernel Schema & RLS Implementation Candidate**.

YQS1 would be allowed to propose:

- local Supabase migrations for the eight schemas;
- table/RPC/RLS definitions for the eighteen objects;
- pgTAP tests and CI;
- a development-only project creation plan;
- a Quant Worker bridge implementation plan.

Actual cloud project creation and production migration remain separate explicit implementation actions.

---

## 5. Explicit non-goals

YQS0 does not approve:

- storing health data in the investment project;
- copying Zhuoliqi proprietary factor formulas into public Canon;
- replacing GitHub receipts with database rows;
- running heavy factor mining inside Edge Functions;
- allowing agents to unrestricted `SELECT/UPDATE` across schemas;
- target prices, recommended position weights or personalized trade instructions;
- broker integration or live execution.

---

## 6. Recommended decision

`PASS` only if all twelve questions pass and machine qualification confirms the frozen counts and authority boundaries.

Reserved acceptance token:

`ACCEPT_YQS0_SUPABASE_SOVEREIGN_RESEARCH_KERNEL_ARCHITECTURE`
