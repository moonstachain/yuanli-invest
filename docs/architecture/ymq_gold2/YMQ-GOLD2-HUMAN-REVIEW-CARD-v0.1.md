# YMQ-GOLD2｜Human Review Card v0.1

**PR:** `#99`
**Current state:** `AWAITING_HUMAN_GATE`
**Merge:** not authorized
**Live scheduler:** not authorized
**Capital / sizing / execution:** not authorized

## Gate A｜Constitution

Decision token:

`G0_ACCEPT_GOLD2_CONSTITUTION`

Accept only if you agree that:

- Gold is the first Yuanli macro-quant Genesis Benchmark;
- YG100 × YMQ4 × YIOS converge into one Gold research state rather than a fourth Gold system;
- YMQ4-B3 `DYNAMIC_BETA_DOES_NOT_BEAT_B2` remains scientific history and may not be retuned into success;
- Property Drift is descriptive research, not alpha;
- UNKNOWN / INDETERMINATE / UNIDENTIFIABLE are valid outputs.

## Gate B｜Property Drift benchmark

Decision token:

`G1_ACCEPT_PROPERTY_DRIFT_BENCHMARK`

Machine evidence currently says:

- GFC 2007–2009 = `DRIFT_CONFIRMED_RESEARCH_ONLY`;
- 2010–2019 = `DRIFT_CANDIDATE`;
- 2020–2022 = `DRIFT_CANDIDATE`;
- 2023–2026-08 = `DRIFT_CANDIDATE`.

Acceptance means only that this diagnostic becomes a governed research capability candidate. It does not mean it predicts Gold returns.

## Gate C｜Historical Replay core

Decision token:

`G4_ACCEPT_HISTORICAL_REPLAY_SETTLEMENT`

Accept only if you agree that the eight-regime PIT core Replay is scientifically useful **despite** leaving Expectation × Reality and Valuation × Driver unresolved where data is absent.

Acceptance must not convert missing evidence into positive claims.

## Gate D｜Human triangulation evidence

Decision token:

`G5_SUPPLY_HUMAN_JUDGMENTS`

Required human material:

1. confirm which recent Get-note anonymous speaker segments are actually Liu Yiru;
2. freeze RAY's independent Gold regime judgment with `known_as_of`, confidence and falsifiers;
3. freeze Yiru's independent Gold timing judgment with `known_as_of`, confidence and falsifiers.

The machine must not infer or backfill either human judgment.

## Gate E｜30-Day Live Shadow

Decision token:

`G6_AUTHORIZE_LIVE_SHADOW_SCHEDULER`

If separately authorized later, the runner may gather live evidence and emit research-only states for 30 days. It still may not size positions, connect a broker, invoke VeighNa, or create orders.

## Gate F｜Merge / Canon

This is a separate final gate after all accepted prior gates and fresh exact-head CI.

Possible future token:

`ACCEPT_YMQ_GOLD2_MERGE`

Do not infer it from any earlier acceptance.

## Suggested review order

`G0 → G1 → G4 → G5 → G6 → final merge`

Each gate is independent. Acceptance of one does not imply the next.
